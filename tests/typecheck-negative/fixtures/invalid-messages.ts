import type { WebviewFixture } from "../../visual/recorder/webviewFixtureTypes";

const invalidMessagesFixture: WebviewFixture = {
    schemaVersion: 1,
    contextId: "commit-panel",
    scenario: "invalid-messages",
    messages: "not-an-array",
};

export default invalidMessagesFixture;
