<svg width="24" height="24" viewBox="0 0 128 128" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Circle border -->
  <circle cx="64" cy="64" r="58" stroke="currentColor" stroke-width="5" fill="none"/>
  <!-- Main trunk - bottom to top -->
  <line x1="50" y1="105" x2="50" y2="25" stroke="currentColor" stroke-width="6" stroke-linecap="round"/>
  <!-- Branch curving right and upward -->
  <path d="M50 75 C60 75 80 65 85 45" stroke="currentColor" stroke-width="6" fill="none" stroke-linecap="round"/>
  <!-- Dot - bottom (root) -->
  <circle cx="50" cy="105" r="10" fill="currentColor"/>
  <!-- Dot - top (head) -->
  <circle cx="50" cy="25" r="10" fill="currentColor"/>
  <!-- Dot - branch tip -->
  <circle cx="85" cy="45" r="10" fill="currentColor"/>
</svg>
